const { Given, When, Then } = require('cucumber');


Given('AdminEventHome component is rendered with default properties', function () {
});

When('the ToggleButtonGroup changes with value {string} and mock response is successful', function (value) {

});

When('the ToggleButtonGroup changes with value {string} and mock response fails with status {int}', function (value, status) {

});

Then('the rendered component should match the snapshot', function () {

});